[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "telescraper"
dynamic = ["version"]
description = "Terminal tool for scraping and analysing Telegram channels, groups and chats"
requires-python = ">=3.11"
dependencies = [
    "telethon<2",
    "python-dotenv",
    "pandas",
    "numpy",
    "tqdm",
    "pyarrow",
    "openpyxl",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
telescraper = "telescraper.cli:main"

[tool.setuptools]
packages = ["telescraper"]

[tool.setuptools.dynamic]
version = {attr = "telescraper.__version__"}
